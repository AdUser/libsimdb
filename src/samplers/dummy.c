#include "../common.h"
#include "../simdb.h"

bool
simdb_record_create(simdb_rec_t * const rec, const char * const source) {
  assert(rec != NULL);
  assert(source != NULL);

  (void)(rec);
  (void)(source);

  return false;
}
